name: iOS CI

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    name: Swift Build
    runs-on: macos-15
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: swift build
        run: swift build --target OpenCodeRemote 2>&1 | tail -80
